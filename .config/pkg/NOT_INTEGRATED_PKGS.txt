# i think there is no direct url or a repo to pre-build binaries for those
ffmpeg
mpv
zathura
pass # i actually installed it but it's not work!!, see [[utils.kdl]]
htop
btop
gpg
git # yeah, even it's not exist via mise
magick # this is the repo but it's conrain only linux builds "ImageMagick/ImageMagick" // a tool to convert images and work with them
timewarrior # this is the repo but there is no build "GothenburgBitFactory/timewarrior"
posting # the repo "darrenburns/posting", it's only available via the 'nu' python package manager (so it's need an nu bridge but i don't wanna write one for it)

# guis (mac only issue)
kitty
gimp3

# i think those is from the unix coreutils
curl
wget

# this is a dep for almost any c program
openssl
