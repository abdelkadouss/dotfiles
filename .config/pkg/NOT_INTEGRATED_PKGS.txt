# i think there is no direct url or a repo to pre-build binaries for those
ffmpeg
mpv
zathura
htop
btop
posting # the repo "darrenburns/posting", it's only available via the 'uv' python package manager (so it's need an nv bridge but i don't wanna write one for it)

# guis (mac only issue)
kitty
gimp3
